local Maps = {
    complexMap = require(script.Parent:WaitForChild("complexMapLayout")),
    oldLevel = require(script.Parent:WaitForChild("oldLevelLayout")),
    platformMapping = require(script.Parent:WaitForChild("platformMapping")),
    dogsMapping = require(script.Parent:WaitForChild("dogsMapLayout"))
}

return Maps
